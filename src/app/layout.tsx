import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ClientEffects } from "@/components/effects/client-effects";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Zenith Simulation — Advanced Network Training Platform",
  description: "Master network protocols, real-time analysis, and cybersecurity in high-fidelity simulations.",
  keywords: ["Zenith", "Simulation", "Network", "TCP", "UDP", "WebSocket", "Real-time Analysis"],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ClientEffects />
        {children}
        <Toaster />
      </body>
    </html>
  );
}